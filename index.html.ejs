<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <meta name="description" content="<%= process.env.description %>">
        <meta name="keywords" content="Sasin, 70000000, React, online, converter">
        <meta name="author" content="<%= process.env.author %>">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title><%= process.env.title %></title>
        <style type="text/css">
            ${require("style!css!scss!style.scss")}
        </style>
    </head>
    <body>
        <div id="<%= process.env.root %>">
        </div>
    </body>
</html>